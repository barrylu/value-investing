# 长江电力年报知识库校验报告

- 校验一：PDF 页数与全文解析页数一致
- 校验二：目录节次与正文顶层节标题回勾
- 校验三：`excel/*.xls` 的 `仅年报` 列与年报主表标准化结果逐项比对
- 校验四：`2016-2024` 额外引用 `长江电力-2003至2024-财务报表与附注明细-已对账.xlsx` 的 Datayes 对账摘要

## 2003

- PDF 完整性：`77` 页已全部解析；低文本页 `0` 页；页字符数中位数 `913`。
- 目录回勾：命中 `12/12`；目录页码偏移 `5`。
- 主表/附注抽取复用校验：主表页 `利润表:48, 现金流量表:49,50,51,52, 资产负债表:46,47`；附注起始页 `53`；状态 `成功`。
- Excel 对比汇总：一致 `98` 条，差异 `0` 条，未命中 `3` 条。
- 利润表：比较 `19` 条；一致 `19`；差异 `0`；未命中 `0`。
- 资产负债表：比较 `43` 条；一致 `40`；差异 `0`；未命中 `3`。
- 现金流量表：比较 `39` 条；一致 `39`；差异 `0`；未命中 `0`。
- 样例：`资产负债表` `其他应收款(合计):应收股利` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `非流动负债的特殊项目` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `归属于母公司所有者权益的特殊项目` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2004

- PDF 完整性：`77` 页已全部解析；低文本页 `0` 页；页字符数中位数 `927`。
- 目录回勾：命中 `12/12`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:46, 现金流量表:47,48, 资产负债表:44,45`；附注起始页 `49`；状态 `成功`。
- Excel 对比汇总：一致 `95` 条，差异 `0` 条，未命中 `6` 条。
- 利润表：比较 `19` 条；一致 `19`；差异 `0`；未命中 `0`。
- 资产负债表：比较 `41` 条；一致 `39`；差异 `0`；未命中 `2`。
- 现金流量表：比较 `41` 条；一致 `37`；差异 `0`；未命中 `4`。
- 样例：`资产负债表` `其他应收款(合计):应收利息` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `一年内到期的非流动负债` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `吸收投资收到的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `取得借款收到的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `筹资活动现金流入的差错金额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2005

- PDF 完整性：`84` 页已全部解析；低文本页 `0` 页；页字符数中位数 `890.5`。
- 目录回勾：命中 `12/12`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:50,51, 现金流量表:51,52, 资产负债表:48,49,50`；附注起始页 `53`；状态 `成功`。
- Excel 对比汇总：一致 `95` 条，差异 `0` 条，未命中 `3` 条。
- 利润表：比较 `19` 条；一致 `19`；差异 `0`；未命中 `0`。
- 资产负债表：比较 `40` 条；一致 `39`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `39` 条；一致 `37`；差异 `0`；未命中 `2`。
- 样例：`资产负债表` `预收款项` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `收到其他与投资活动有关的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `支付其他与投资活动有关的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2006

- PDF 完整性：`97` 页已全部解析；低文本页 `0` 页；页字符数中位数 `896`。
- 目录回勾：命中 `12/12`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:58,59, 现金流量表:59,60, 资产负债表:56,57,58`；附注起始页 `61`；状态 `成功`。
- Excel 对比汇总：一致 `88` 条，差异 `0` 条，未命中 `20` 条。
- 利润表：比较 `26` 条；一致 `20`；差异 `0`；未命中 `6`。
- 资产负债表：比较 `44` 条；一致 `40`；差异 `0`；未命中 `4`。
- 现金流量表：比较 `38` 条；一致 `28`；差异 `0`；未命中 `10`。
- 样例：`利润表` `影响营业利润的特殊项目` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `影响利润总额的特殊项目` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.44` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.43` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `综合收益总额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2007

- PDF 完整性：`119` 页已全部解析；低文本页 `0` 页；页字符数中位数 `997`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:71,72, 现金流量表:72, 资产负债表:69,70,71`；附注起始页 `75`；状态 `成功`。
- Excel 对比汇总：一致 `89` 条，差异 `0` 条，未命中 `3` 条。
- 利润表：比较 `22` 条；一致 `19`；差异 `0`；未命中 `3`。
- 资产负债表：比较 `41` 条；一致 `41`；差异 `0`；未命中 `0`。
- 现金流量表：比较 `29` 条；一致 `29`；差异 `0`；未命中 `0`。
- 样例：`利润表` `公允价值变动收益` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.6` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.58` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2008

- PDF 完整性：`133` 页已全部解析；低文本页 `0` 页；页字符数中位数 `941`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:55,56, 现金流量表:56,57, 资产负债表:53,54,55`；附注起始页 `61`；状态 `成功`。
- Excel 对比汇总：一致 `102` 条，差异 `0` 条，未命中 `3` 条。
- 利润表：比较 `28` 条；一致 `26`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `47` 条；一致 `47`；差异 `0`；未命中 `0`。
- 现金流量表：比较 `30` 条；一致 `29`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.42` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.42` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `吸收投资收到的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2009

- PDF 完整性：`194` 页已全部解析；低文本页 `0` 页；页字符数中位数 `941.0`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:65,66, 现金流量表:66, 资产负债表:63,64,65`；附注起始页 `71`；状态 `成功`。
- Excel 对比汇总：一致 `104` 条，差异 `0` 条，未命中 `2` 条。
- 利润表：比较 `28` 条；一致 `26`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `49` 条；一致 `49`；差异 `0`；未命中 `0`。
- 现金流量表：比较 `29` 条；一致 `29`；差异 `0`；未命中 `0`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.31` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.31` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2010

- PDF 完整性：`140` 页已全部解析；低文本页 `0` 页；页字符数中位数 `1040.5`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:65,66, 现金流量表:66, 资产负债表:63,64,65`；附注起始页 `71`；状态 `成功`。
- Excel 对比汇总：一致 `104` 条，差异 `0` 条，未命中 `4` 条。
- 利润表：比较 `28` 条；一致 `25`；差异 `0`；未命中 `3`。
- 资产负债表：比较 `51` 条；一致 `50`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `29` 条；一致 `29`；差异 `0`；未命中 `0`。
- 样例：`利润表` `其中:被合并方在合并前实现的净利润` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.5` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.5` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应付款(合计):应付股利` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2011

- PDF 完整性：`140` 页已全部解析；低文本页 `0` 页；页字符数中位数 `1032.0`。
- 目录回勾：命中 `11/11`；目录页码偏移 `4`。
- 主表/附注抽取复用校验：主表页 `利润表:70,71, 现金流量表:71, 资产负债表:68,69,70`；附注起始页 `76`；状态 `成功`。
- Excel 对比汇总：一致 `105` 条，差异 `0` 条，未命中 `4` 条。
- 利润表：比较 `27` 条；一致 `25`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `52` 条；一致 `51`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `30` 条；一致 `29`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.47` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.47` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):应收股利` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `其中:子公司支付给少数股东的股利、利润` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2012

- PDF 完整性：`126` 页已全部解析；低文本页 `0` 页；页字符数中位数 `1114.0`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:50,51, 现金流量表:51,52, 资产负债表:48,49,50`；附注起始页 `56`；状态 `成功`。
- Excel 对比汇总：一致 `106` 条，差异 `0` 条，未命中 `4` 条。
- 利润表：比较 `27` 条；一致 `25`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `51` 条；一致 `49`；差异 `0`；未命中 `2`。
- 现金流量表：比较 `32` 条；一致 `32`；差异 `0`；未命中 `0`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.63` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.63` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):应收利息` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他流动资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2013

- PDF 完整性：`129` 页已全部解析；低文本页 `0` 页；页字符数中位数 `1097`。
- 目录回勾：命中 `11/11`；目录页码偏移 `3`。
- 主表/附注抽取复用校验：主表页 `利润表:49,50, 现金流量表:50,51, 资产负债表:47,48,49`；附注起始页 `55`；状态 `成功`。
- Excel 对比汇总：一致 `105` 条，差异 `0` 条，未命中 `7` 条。
- 利润表：比较 `27` 条；一致 `25`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `52` 条；一致 `49`；差异 `0`；未命中 `3`。
- 现金流量表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.55` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.55` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `交易性金融资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `长期待摊费用` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `外币报表折算差额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2014

- PDF 完整性：`149` 页已全部解析；低文本页 `0` 页；页字符数中位数 `1070`。
- 目录回勾：命中 `11/11`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:56,57,58, 合并现金流量表:59,60,61, 合并资产负债表:51,52,53,54, 母公司利润表:58,59, 母公司现金流量表:61,62,63, 母公司资产负债表:54,55,56`；附注起始页 `90`；状态 `成功`。
- Excel 对比汇总：一致 `102` 条，差异 `0` 条，未命中 `6` 条。
- 利润表：比较 `27` 条；一致 `24`；差异 `0`；未命中 `3`。
- 资产负债表：比较 `49` 条；一致 `47`；差异 `0`；未命中 `2`。
- 现金流量表：比较 `32` 条；一致 `31`；差异 `0`；未命中 `1`。
- 样例：`利润表` `公允价值变动收益` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.72` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.72` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):应收股利` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `短期借款` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2015

- PDF 完整性：`157` 页已全部解析；低文本页 `0` 页；页字符数中位数 `974`。
- 目录回勾：命中 `12/12`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:50,51, 合并现金流量表:52,53,54, 合并资产负债表:46,47,48, 母公司利润表:51,52, 母公司现金流量表:54,55,56, 母公司资产负债表:48,49,50`；附注起始页 `90`；状态 `成功`。
- Excel 对比汇总：一致 `110` 条，差异 `0` 条，未命中 `4` 条。
- 利润表：比较 `27` 条；一致 `25`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `53` 条；一致 `52`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `34` 条；一致 `33`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.83` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.83` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他非流动资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `处置子公司及其他营业单位收到的现金净额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。

## 2016

- PDF 完整性：`193` 页已全部解析；低文本页 `0` 页；页字符数中位数 `842`。
- 目录回勾：命中 `12/12`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:64,65, 合并现金流量表:66,67,68, 合并资产负债表:60,61,62, 母公司利润表:65,66, 母公司现金流量表:68,69,70, 母公司资产负债表:62,63,64`；附注起始页 `99`；状态 `成功`。
- Excel 对比汇总：一致 `117` 条，差异 `0` 条，未命中 `5` 条。
- 利润表：比较 `30` 条；一致 `27`；差异 `0`；未命中 `3`。
- 资产负债表：比较 `57` 条；一致 `56`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `35` 条；一致 `34`；差异 `0`；未命中 `1`。
- 样例：`利润表` `其中:非流动资产处置损失` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.94` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.94` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):应收股利` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `收到其他与筹资活动有关的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 107 项；与后续年报重述值一致 6 项；与Datayes存在差异，保留PDF原值 5 项。

## 2017

- PDF 完整性：`206` 页已全部解析；低文本页 `0` 页；页字符数中位数 `780.5`。
- 目录回勾：命中 `12/12`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:67,68,69, 合并现金流量表:71,72,73, 合并资产负债表:63,64,65, 母公司利润表:69,70,71, 母公司现金流量表:73,74,75, 母公司资产负债表:65,66,67`；附注起始页 `106`；状态 `成功`。
- Excel 对比汇总：一致 `119` 条，差异 `0` 条，未命中 `5` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `57` 条；一致 `55`；差异 `0`；未命中 `2`。
- 现金流量表：比较 `34` 条；一致 `33`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.01` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.01` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):应收利息` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `持有待售资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `取得子公司及其他营业单位支付的现金净额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 108 项；与后续年报重述值一致 12 项。

## 2018

- PDF 完整性：`197` 页已全部解析；低文本页 `0` 页；页字符数中位数 `863`。
- 目录回勾：命中 `12/12`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:60,61, 合并现金流量表:62,63,64, 合并资产负债表:56,57,58, 母公司利润表:61,62, 母公司现金流量表:64,65,66, 母公司资产负债表:58,59,60`；附注起始页 `93`；状态 `成功`。
- Excel 对比汇总：一致 `118` 条，差异 `0` 条，未命中 `5` 条。
- 利润表：比较 `33` 条；一致 `30`；差异 `0`；未命中 `3`。
- 资产负债表：比较 `57` 条；一致 `56`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `33` 条；一致 `32`；差异 `0`；未命中 `1`。
- 样例：`利润表` `终止经营净利润` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.03` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.03` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `交易性金融资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `支付其他与投资活动有关的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 111 项；与后续年报重述值一致 4 项；与Datayes存在差异，保留PDF原值 3 项；PDF未定位到，已用Datayes补充 1 项。

## 2019

- PDF 完整性：`198` 页已全部解析；低文本页 `0` 页；页字符数中位数 `928.5`。
- 目录回勾：命中 `12/12`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:61,62,63, 合并现金流量表:65,66, 合并资产负债表:57,58,59,93,94,95,96, 母公司利润表:63,64,65, 母公司现金流量表:66,67,68, 母公司资产负债表:59,60,61,96,97,98`；附注起始页 `99`；状态 `成功`。
- Excel 对比汇总：一致 `119` 条，差异 `0` 条，未命中 `6` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `60` 条；一致 `59`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `32` 条；一致 `29`；差异 `0`；未命中 `3`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.98` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.98` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `可供出售金融资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `收到的税费返还` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `吸收投资收到的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 112 项；与Datayes存在差异，保留PDF原值 6 项；PDF未定位到，已用Datayes补充 2 项。

## 2020

- PDF 完整性：`215` 页已全部解析；低文本页 `0` 页；页字符数中位数 `881`。
- 目录回勾：命中 `11/11`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:66,67,68, 合并现金流量表:69,70,71, 合并资产负债表:61,62,63,64,99,100,101, 母公司利润表:68,69, 母公司现金流量表:71,72,73, 母公司资产负债表:64,65,66,101,102,103,104`；附注起始页 `105`；状态 `成功`。
- Excel 对比汇总：一致 `128` 条，差异 `0` 条，未命中 `2` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `63` 条；一致 `63`；差异 `0`；未命中 `0`。
- 现金流量表：比较 `34` 条；一致 `34`；差异 `0`；未命中 `0`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.19` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.19` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 121 项；与Datayes存在差异，保留PDF原值 6 项；PDF未定位到，已用Datayes补充 2 项。

## 2021

- PDF 完整性：`222` 页已全部解析；低文本页 `0` 页；页字符数中位数 `848.5`。
- 目录回勾：命中 `10/10`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:79,80,81, 合并现金流量表:83,84,85, 合并资产负债表:75,76,77,118, 母公司利润表:81,82,83, 母公司现金流量表:85,86,87, 母公司资产负债表:77,78,79,118,119,120`；附注起始页 `121`；状态 `成功`。
- Excel 对比汇总：一致 `126` 条，差异 `0` 条，未命中 `6` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `65` 条；一致 `63`；差异 `0`；未命中 `2`。
- 现金流量表：比较 `34` 条；一致 `32`；差异 `0`；未命中 `2`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.16` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.16` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `交易性金融负债` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `预收款项` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `取得子公司及其他营业单位支付的现金净额` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 119 项；与Datayes存在差异，保留PDF原值 6 项；PDF未定位到，已用Datayes补充 2 项。

## 2022

- PDF 完整性：`236` 页已全部解析；低文本页 `0` 页；页字符数中位数 `918.0`。
- 目录回勾：命中 `10/10`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:92,93,94, 合并现金流量表:95,96,97, 合并资产负债表:88,89,90, 母公司利润表:94,95, 母公司现金流量表:97,98,99,100, 母公司资产负债表:90,91,92`；附注起始页 `137`；状态 `成功`。
- Excel 对比汇总：一致 `127` 条，差异 `0` 条，未命中 `6` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `65` 条；一致 `62`；差异 `0`；未命中 `3`。
- 现金流量表：比较 `35` 条；一致 `34`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `0.97` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `0.97` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应付款(合计):应付利息` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `长期应付款(合计)` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `长期应付款(合计):长期应付款` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与后续年报重述值一致 75 项；与当年PDF一致 39 项；与Datayes存在差异，保留PDF原值 11 项；PDF未定位到，已用Datayes补充 3 项。

## 2023

- PDF 完整性：`265` 页已全部解析；低文本页 `0` 页；页字符数中位数 `949`。
- 目录回勾：命中 `10/10`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:92,93,94, 合并现金流量表:95,96,97, 合并资产负债表:88,89,90,144,145,146, 母公司利润表:94,95, 母公司现金流量表:97,98,99,100, 母公司资产负债表:90,91,92,146,147,148`；附注起始页 `149`；状态 `成功`。
- Excel 对比汇总：一致 `130` 条，差异 `0` 条，未命中 `4` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `64` 条；一致 `63`；差异 `0`；未命中 `1`。
- 现金流量表：比较 `37` 条；一致 `36`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.11` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.11` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `应收票据及应收账款:应收票据` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`现金流量表` `支付其他与投资活动有关的现金` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与后续年报重述值一致 76 项；与当年PDF一致 48 项；与Datayes存在差异，保留PDF原值 5 项；PDF未定位到，已用Datayes补充 2 项。

## 2024

- PDF 完整性：`262` 页已全部解析；低文本页 `0` 页；页字符数中位数 `927.0`。
- 目录回勾：命中 `10/10`；目录页码偏移 `0`。
- 主表/附注抽取复用校验：主表页 `合并利润表:92,93,94, 合并现金流量表:95,96,97, 合并资产负债表:88,89,90, 母公司利润表:94,95, 母公司现金流量表:97,98,99, 母公司资产负债表:90,91,92`；附注起始页 `142`；状态 `成功`。
- Excel 对比汇总：一致 `122` 条，差异 `0` 条，未命中 `10` 条。
- 利润表：比较 `33` 条；一致 `31`；差异 `0`；未命中 `2`。
- 资产负债表：比较 `63` 条；一致 `56`；差异 `0`；未命中 `7`。
- 现金流量表：比较 `36` 条；一致 `35`；差异 `0`；未命中 `1`。
- 样例：`利润表` `每股收益-基本` -> 未命中；Excel 当前值 `1.33` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`利润表` `每股收益-稀释` -> 未命中；Excel 当前值 `1.33` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `衍生金融资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `其他应收款(合计):其他应收款` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- 样例：`资产负债表` `合同资产` -> 未命中；Excel 当前值 `None` 百万元；PDF 匹配 ``；PDF 当前值 `None` 百万元。
- Datayes 对账摘要：与当年PDF一致 96 项；与Datayes存在差异，保留PDF原值 25 项；PDF未定位到，已用Datayes补充 1 项；待复核 1 项。
